# Pinned exactly. The Docker image is rebuilt on every deploy, so a floating
# range meant a rebuild could silently pull a breaking upstream major (pydantic,
# anthropic) and fail in production rather than in CI. Bump these deliberately.
fastapi==0.138.1
uvicorn[standard]==0.49.0
asyncpg==0.31.0
anthropic==0.112.0
httpx==0.28.1
pydantic==2.13.4
argon2-cffi==25.1.0
cryptography==49.0.0
sentry-sdk==2.63.0
redis==8.0.1
